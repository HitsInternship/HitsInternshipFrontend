export interface ILoginData {
  phone: string;
  password: string;
}
export type TUserRole = 'student' | 'employee';

export interface IUser {
  name: string;
  surname: string;
  email: string;
}
